import { animateText } from './textAnimation.js';
import { addScanEffects } from './visualEffects.js';

document.addEventListener('DOMContentLoaded', () => {
    // Iniciar la animación de texto
    animateText();
    
    // Añadir efectos visuales
    addScanEffects();
});